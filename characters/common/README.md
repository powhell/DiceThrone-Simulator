# Cartes communes

Dépose ici le PDF/scans du deck de cartes partagé par tous les héros (~18 cartes communes
sur les 32 de chaque deck — le reste est spécifique au héros).

Claude Code pourra le lire avec le tool `Read` pour remplir
`engine-ts/src/sim/data/common-cards.json`.
